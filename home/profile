[ -f "$HOME/.path" ] && source ~/.path
[ -f "$HOME/.exports" ] && source ~/.exports

# Not version controlled for machine specific
# configuration.
[ -f "$HOME/.profile_extra" ] && source ~/.profile_extra

